from setuptools import setup

setup(
   name='finetune_embedding_model',
   version='1.0',
   description='A useful module',
   author='Faith',
   author_email='',
   packages=['finetune_embedding_model'],

)